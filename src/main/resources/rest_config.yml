clients:
  users:
    base-url: ${USERS_BASE_URL}
    connection-timeout: 5000
    socket-timeout: 5000
  places:
    base-url: ${PLACES_BASE_URL}
    connection-timeout: 5000
    socket-timeout: 5000

logging:
  level:
    org.springframework.security: DEBUG